name: StreamsGate Updater

on:
  schedule:
    - cron: '*/30 * * * *' # Berjalan otomatis setiap 30 menit
  workflow_dispatch: # Tombol manual jalan

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          # Sangat ringan! Tidak butuh Playwright/Chromium sama sekali
          pip install httpx beautifulsoup4

      - name: Run Scraper
        run: |
          echo "Memulai eksekusi StreamsGate..."
          python streamsgate.py

      - name: Commit & Push safely
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

          # Cek dulu apakah file berhasil dibuat oleh script Python
          if [ -f streamsgate.m3u8 ]; then
            git add streamsgate.m3u8
          else
            echo "File streamsgate.m3u8 tidak ditemukan (Mungkin tidak ada jadwal saat ini). Skip commit."
            exit 0
          fi

          # Cek apakah ada perbedaan isi file (jadwal baru)
          if git diff --cached --quiet; then
            echo "Tidak ada jadwal baru, skip commit."
            exit 0
          fi

          git commit -m "Update StreamsGate playlist [skip ci]"

          # Logika anti-tabrakan (rebase) andalan MABES ENTERPRISE
          if ! git push origin main; then
            echo "Push tertahan — melakukan rebase aman..."
            git stash
            git pull --rebase origin main
            git stash pop
            git add streamsgate.m3u8 || true
            git commit -m "Update StreamsGate playlist (rebased) [skip ci]" || true
            git push origin main
          fi
